<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/css/info.css">
        <title>Next Episode</title>
    </head>

    <body>
        <div class="show-card">
            <img src="{{showImage}}" alt="{{showName}}">
            <div class="show-info">
                <h4>{{showName}}</h4>
                <div class="tags">
                    {{#each tags}}
                    <h6>{{this}}</h6>
                    {{/each}}
                </div>
                <h5>{{summary}}</h5>
                {{#if imdb}}
                <a id="imdb" href="https://www.imdb.com/title/{{imdb}}/">
                    <i class="fab fa-imdb"></i>
                    <p>Find on IMDB</p>
                </a>
                {{/if}}
                <form id="myform" method="post" action="/shows">
                    <input type="hidden" id="id" name="id" value="{{id}}">
                    <input type="hidden" id="name" name="name" value="{{showName}}">
                    <input type="hidden" id="image" name="image" value="{{showImage}}">
                    <input type="hidden" id="rating" name="rating" value="{{rating}}">
                    <button type="submit" class="btn btn-success">
                        <i class="fa fa-heart"></i>
                    </button>
                </form>
            </div>
        </div>
    </body>

</html>